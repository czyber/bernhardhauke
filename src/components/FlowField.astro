<canvas class="flow-field" aria-hidden="true"></canvas>

<script>
  import { startAmbientScene } from "./ambientScene";
  const canvas = document.querySelector<HTMLCanvasElement>(".flow-field");
  if (canvas) startAmbientScene(canvas);
</script>

<style>
  .flow-field {
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100dvh;
    z-index: 0;
    pointer-events: none;
    opacity: 0;
  }
  .flow-field[data-ready] {
    opacity: 1;
  }
  @media (prefers-reduced-motion: reduce) {
    .flow-field {
      transition: none;
    }
  }
</style>
